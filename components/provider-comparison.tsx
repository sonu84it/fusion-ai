import Link from "next/link";
import { ArrowLeft, ExternalLink, Scale, Sparkles } from "lucide-react";
import { type Provider } from "@/lib/providers";

export function ProviderComparison({ selected }: { selected: Provider[] }) {
  return <main className="page-shell min-h-screen"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_20%_0%,rgba(6,182,212,.13),transparent_28%)]"/><nav className="relative z-10 mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/" className="flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft className="h-4 w-4"/>Provider directory</Link><Link href="/" className="rounded-xl border border-white/10 bg-white/[.04] px-3 py-2 text-xs font-semibold text-slate-200">Browse all providers</Link></nav><section className="relative z-10 mx-auto max-w-7xl px-5 pb-24 pt-10 lg:px-8"><div className="max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[.16em] text-cyan-300">PROVIDER COMPARISON</p><h1 className="font-display mt-3 text-4xl font-semibold tracking-[-.05em] sm:text-5xl">Compare official model sources.</h1><p className="mt-4 text-sm leading-6 text-slate-400">This directory intentionally links to each organization&apos;s own model, price, and benchmark pages so decisions can use current information rather than copied claims.</p></div><div className="mt-9 overflow-x-auto rounded-3xl border border-white/[.09] bg-[#0b1124]/75"><table className="min-w-[760px] w-full border-collapse text-left"><thead><tr className="border-b border-white/[.08]"><th className="w-[190px] px-5 py-4 text-[10px] font-semibold uppercase tracking-[.14em] text-slate-600">Provider</th>{selected.map((provider) => <th key={provider.slug} className="min-w-[190px] px-5 py-4"><div className="flex items-center gap-2"><span className="flex h-7 w-7 items-center justify-center rounded-lg text-[9px] font-semibold" style={{ background: provider.colors.soft, color: provider.colors.primary }}>{provider.shortName}</span><span className="font-display text-sm font-semibold">{provider.name}</span></div></th>)}</tr></thead><tbody>{[["Access", (provider: Provider) => provider.access], ["Model families", (provider: Provider) => provider.models.join(", ")], ["Open-model option", (provider: Provider) => provider.openSource ? "Yes" : "No"], ["Official pricing", () => "See provider source"], ["Context & benchmarks", () => "See model documentation"]].map(([label, getValue]) => <tr key={label as string} className="border-b border-white/[.06] last:border-0"><td className="bg-white/[.018] px-5 py-4 text-xs font-medium text-slate-400">{label as string}</td>{selected.map((provider) => <td key={provider.slug} className="px-5 py-4 text-xs leading-5 text-slate-300">{(getValue as (provider: Provider) => string)(provider)}</td>)}</tr>)}</tbody></table></div><div className="mt-6 grid gap-3 md:grid-cols-2">{selected.map((provider) => <article key={provider.slug} className="rounded-2xl border border-white/[.08] bg-[#0b1124]/70 p-4"><div className="flex items-center justify-between"><h2 className="font-display font-semibold">{provider.name}</h2><Sparkles className="h-4 w-4" style={{ color: provider.colors.primary }}/></div><p className="mt-2 text-xs leading-5 text-slate-500">{provider.overview}</p><div className="mt-4 flex gap-2"><a href={provider.docs} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 rounded-lg border border-white/[.08] px-2.5 py-1.5 text-[11px] text-slate-300 hover:bg-white/[.05]">Docs <ExternalLink className="h-3 w-3"/></a><Link href={`/providers/${provider.slug}`} className="inline-flex items-center gap-1.5 rounded-lg border border-white/[.08] px-2.5 py-1.5 text-[11px] text-slate-300 hover:bg-white/[.05]">Profile <Scale className="h-3 w-3"/></Link></div></article>)}</div></section></main>;
}
