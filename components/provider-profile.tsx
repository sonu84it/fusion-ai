import Link from "next/link";
import { ArrowLeft, ArrowRight, BookOpen, ExternalLink, FileText, Globe2, MapPin, Sparkles } from "lucide-react";
import { ModelLaunchTimeline } from "@/components/model-launch-timeline";
import { modelLaunches, type Provider } from "@/lib/providers";

export function ProviderProfile({ provider }: { provider: Provider }) {
  const launchHistory = [...modelLaunches[provider.slug]].sort((left, right) => left.date.localeCompare(right.date));
  const firstDocumentedRelease = launchHistory[0];
  const latestDocumentedRelease = launchHistory[launchHistory.length - 1];
  return <main className="page-shell min-h-screen"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_80%_0%,rgba(124,58,237,.16),transparent_30%)]"/><nav className="relative z-10 mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/" className="flex items-center gap-2 text-sm text-slate-400 transition hover:text-white"><ArrowLeft className="h-4 w-4"/>Provider directory</Link><Link href={`/compare?providers=${provider.slug},openai`} className="rounded-xl border border-white/10 bg-white/[.04] px-3 py-2 text-xs font-semibold text-slate-200 hover:bg-white/[.08]">Compare provider</Link></nav><section className="relative z-10 mx-auto max-w-5xl px-5 pb-24 pt-12 lg:px-8"><div className="rounded-[30px] border border-white/[.1] bg-[#0b1124]/82 p-6 shadow-2xl sm:p-10"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-start"><div className="flex gap-4"><span className="flex h-14 w-14 shrink-0 items-center justify-center rounded-2xl border border-white/10 text-sm font-semibold" style={{ background: provider.colors.soft, color: provider.colors.primary }}>{provider.shortName}</span><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-violet-300">{provider.category}</p><h1 className="font-display mt-2 text-4xl font-semibold tracking-[-.05em]">{provider.name}</h1><p className="mt-2 max-w-xl text-sm leading-6 text-slate-400">{provider.overview}</p></div></div><div className="flex shrink-0 gap-2"><a href={provider.website} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 rounded-xl bg-white px-3 py-2 text-xs font-semibold text-slate-950">Website <ExternalLink className="h-3.5 w-3.5"/></a><a href={provider.docs} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 rounded-xl border border-white/10 bg-white/[.04] px-3 py-2 text-xs font-semibold text-slate-200 hover:bg-white/[.08]">API docs <BookOpen className="h-3.5 w-3.5"/></a></div></div><div className="mt-10 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-white/[.08] bg-white/[.025] p-4"><MapPin className="h-4 w-4 text-cyan-300"/><p className="mt-3 text-[10px] uppercase tracking-[.13em] text-slate-600">Headquarters</p><p className="mt-1 text-sm text-slate-300">{provider.headquarters}</p></div><div className="rounded-xl border border-white/[.08] bg-white/[.025] p-4"><Sparkles className="h-4 w-4 text-violet-300"/><p className="mt-3 text-[10px] uppercase tracking-[.13em] text-slate-600">Access</p><p className="mt-1 text-sm text-slate-300">{provider.access}</p></div><div className="rounded-xl border border-white/[.08] bg-white/[.025] p-4"><Globe2 className="h-4 w-4 text-pink-300"/><p className="mt-3 text-[10px] uppercase tracking-[.13em] text-slate-600">Founded</p><p className="mt-1 text-sm text-slate-300">{provider.founded}</p></div></div></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.25fr_.75fr]"><section className="rounded-3xl border border-white/[.08] bg-[#0b1124]/72 p-6"><p className="text-xs font-semibold uppercase tracking-[.16em] text-cyan-300">AVAILABLE MODEL FAMILIES</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{provider.models.map((model) => <div key={model} className="rounded-xl border border-white/[.08] bg-white/[.025] p-4"><FileText className="h-4 w-4" style={{ color: provider.colors.primary }}/><h2 className="font-display mt-3 text-lg font-semibold">{model}</h2><p className="mt-1 text-xs leading-5 text-slate-500">See the official model page for current variants, supported modalities, context window, and pricing.</p></div>)}</div></section><aside className="rounded-3xl border border-violet-400/15 bg-violet-500/[.07] p-6"><p className="text-xs font-semibold uppercase tracking-[.16em] text-violet-200">LIVE REFERENCE</p><p className="mt-3 text-sm leading-6 text-slate-300">{provider.notes}</p><div className="mt-5 grid gap-2"><div className="rounded-xl border border-white/[.08] bg-[#0b1124]/70 p-3"><p className="text-[10px] font-semibold uppercase tracking-[.13em] text-slate-500">Documented releases</p><p className="mt-1 text-lg font-semibold text-slate-100">{launchHistory.length}</p><p className="text-xs text-slate-500">First cataloged: {firstDocumentedRelease.label} ({firstDocumentedRelease.date.slice(0, 4)})</p></div><div className="rounded-xl border border-white/[.08] bg-[#0b1124]/70 p-3"><p className="text-[10px] font-semibold uppercase tracking-[.13em] text-slate-500">Latest documented release</p><p className="mt-1 text-sm font-semibold text-slate-100">{latestDocumentedRelease.label}</p><p className="text-xs text-slate-500">{latestDocumentedRelease.date}</p></div></div><div className="mt-6 space-y-2"><a href={provider.docs} target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-xl border border-white/10 bg-[#0b1124]/80 px-3 py-3 text-xs font-semibold text-slate-200">Model releases & pricing <ArrowRight className="h-3.5 w-3.5 text-violet-300"/></a>{provider.chatUrl && <a href={provider.chatUrl} target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-xl border border-white/10 bg-white/[.04] px-3 py-3 text-xs font-semibold text-slate-200">Open official chat <ExternalLink className="h-3.5 w-3.5 text-violet-300"/></a>}<Link href={`/compare?providers=${provider.slug},openai,anthropic`} className="flex items-center justify-between rounded-xl bg-white px-3 py-3 text-xs font-semibold text-slate-950">Compare against others <ArrowRight className="h-3.5 w-3.5"/></Link></div></aside></div><div className="mt-6"><ModelLaunchTimeline provider={provider} launches={launchHistory} /></div></section></main>;
}
